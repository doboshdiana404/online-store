export enum Sizes {
  ExtraSmall = 'xs',
  Small = 's',
  Medium = 'm',
  Large = 'l',
}

export enum Variant {
  Basic = 'basic',
  Primary = 'primary',
  Link = 'link',
  Icon = 'icon',
  Card = 'card',
  InCard = 'inCard',
  Slider = 'slider',
  Burger = 'burger',
  Filter = 'filter',
  FilterActive = 'filter_active',
  ClearFilter = 'clear_filter',
}

export enum Position {
  Left = 'left',
  Right = 'right',
}
