export enum Sizes {
  ExtraSmall = 'xs',
  Small = 's',
  Medium = 'm',
  Large = 'l',
}

export enum Variant {
  Basic = 'basic',
  Primary = 'primary',
}

export enum Position {
  Left = 'left',
  Right = 'right',
}
